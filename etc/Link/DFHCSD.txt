 DEFINE PROGRAM(EDUCHAN) GROUP(JCICSAMP)
        DESCRIPTION(JCICS LINK sample backend)
        JVM(YES) JVMCLASS(com.ibm.cicsdev.link.LinkServEduchan)
        EXECKEY(CICS) CONCURRENCY(REQUIRED) API(CICSAPI) 
        JVMSERVER(DFHOSGI)  

 DEFINE PROGRAM(JAVALIN1) GROUP(JCICSAMP)
        DESCRIPTION(JCICS LINK sample)
        JVMCLASS(com.ibm.cicsdev.link.LinkProg1) JVMSERVER(DFHJVMS)
        JVM(YES) JVMCLASS(com.ibm.cicsdev.link.LinkProg1)
        EXECKEY(CICS) CONCURRENCY(REQUIRED) API(CICSAPI) 
        JVMSERVER(DFHOSGI) 

 DEFINE PROGRAM(JAVALIN2) GROUP(JCICSAMP)
        DESCRIPTION(JCICS LINK sample)
        JVMCLASS(com.ibm.cicsdev.link.LinkProg2) JVMSERVER(DFHJVMS)
        JVM(YES) JVMCLASS(com.ibm.cicsdev.link.LinkProg2)
        EXECKEY(CICS) CONCURRENCY(REQUIRED) API(CICSAPI) 
        JVMSERVER(DFHOSGI) 

 DEFINE PROGRAM(JAVALIN3) GROUP(JCICSAMP)        
        DESCRIPTION(JCICS LINK sample)
        JVMCLASS(com.ibm.cicsdev.link.LinkProg3) JVMSERVER(DFHJVMS)
        JVM(YES) JVMCLASS(com.ibm.cicsdev.link.LinkProg3)
        EXECKEY(CICS) CONCURRENCY(REQUIRED) API(CICSAPI) 
        JVMSERVER(DFHOSGI)        

 DEFINE TRANSACTION(JOL1) GROUP(JCICSAMP)
        DESCRIPTION(JCICS LINK sample)
        PROGRAM(JAVALIN1) 

 DEFINE TRANSACTION(JOL2) GROUP(JCICSAMP)
        DESCRIPTION(JCICS LINK sample)
        PROGRAM(JAVALIN2) 

 DEFINE TRANSACTION(JOL3) GROUP(JCICSAMP)
        DESCRIPTION(JCICS LINK sample)
        PROGRAM(JAVALIN3) 

 DEFINE BUNDLE(JCICSLIN) GROUP(JCICSAMP)
        DESCRIPTION(JCICS LINK sample)
        BUNDLEDIR(/u/cics1/com.ibm.cicsdev.link.cicsbundle_1.0.0)
