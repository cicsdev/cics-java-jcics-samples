
  DEFINE PROGRAM(EC01) GROUP(JCICSAMP)
        DESCRIPTION(JCICS LINK sample backend)
        CONCURRENCY(REQUIRED) API(CICSAPI) 
        JVM(YES) JVMSERVER(DFHJVMS) DATALOCATION(ANY)
        EXECKEY(CICS)
        JVMCLASS(com.ibm.cicsdev.link.LinkServEC01)

 DEFINE PROGRAM(EDUCHAN) GROUP(JCICSAMP)
        DESCRIPTION(JCICS LINK sample backend)
        CONCURRENCY(REQUIRED) API(CICSAPI) 
        JVM(YES) JVMSERVER(DFHJVMS) DATALOCATION(ANY)
        EXECKEY(CICS)
        JVMCLASS(com.ibm.cicsdev.link.LinkServEduchan)

 DEFINE PROGRAM(JAVALIN1) GROUP(JCICSAMP)
        DESCRIPTION(JCICS LINK sample)
        CONCURRENCY(REQUIRED) API(CICSAPI) 
        JVM(YES) JVMSERVER(DFHJVMS) DATALOCATION(ANY)
        EXECKEY(CICS)
        JVMCLASS(com.ibm.cicsdev.link.LinkProg1)

 DEFINE PROGRAM(JAVALIN2) GROUP(JCICSAMP)
        DESCRIPTION(JCICS LINK sample)
        CONCURRENCY(REQUIRED) API(CICSAPI) 
        JVM(YES) JVMSERVER(DFHJVMS) DATALOCATION(ANY)
        EXECKEY(CICS)
        JVMCLASS(com.ibm.cicsdev.link.LinkProg2)

 DEFINE PROGRAM(JAVALIN3) GROUP(JCICSAMP)        
        DESCRIPTION(JCICS LINK sample)
        CONCURRENCY(REQUIRED) API(CICSAPI) 
        JVM(YES) JVMSERVER(DFHJVMS) DATALOCATION(ANY)
        EXECKEY(CICS)
        JVMCLASS(com.ibm.cicsdev.link.LinkProg3)

 DEFINE TRANSACTION(JLN1) GROUP(JCICSAMP)
        DESCRIPTION(JCICS LINK sample)
        PROGRAM(JAVALIN1) 
        TASKDATALOC(ANY)

 DEFINE TRANSACTION(JLN2) GROUP(JCICSAMP)
        DESCRIPTION(JCICS LINK sample)
        PROGRAM(JAVALIN2) 
        TASKDATALOC(ANY)

 DEFINE TRANSACTION(JLN3) GROUP(JCICSAMP)
        DESCRIPTION(JCICS LINK sample)
        PROGRAM(JAVALIN3) 
        TASKDATALOC(ANY)

 DEFINE BUNDLE(JCICSLIN) GROUP(JCICSAMP)
        DESCRIPTION(JCICS LINK sample)
        BUNDLEDIR(/u/cics1/com.ibm.cicsdev.link.cicsbundle_1.0.0)
